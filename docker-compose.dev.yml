# Auto-generated by ma-provider-tools — do not edit manually.
# Docker Compose for DLNA Receiver provider dev environment.
services:
  mass:
    image: ghcr.io/trudenboy/ma-server:dev
    restart: unless-stopped
    network_mode: host
    volumes:
      - mass_data:/data
      - ./provider:/tmp/provider:ro
      - ./pyproject.toml:/tmp/pyproject.toml:ro
      - ./scripts/docker-init.sh:/docker-init.sh:ro
    entrypoint: ["/bin/sh", "/docker-init.sh"]

volumes:
  mass_data:
